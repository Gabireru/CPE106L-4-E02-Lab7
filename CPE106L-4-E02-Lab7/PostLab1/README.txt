Notes Before running the Program:

1.) Make sure that "pandas" is installed.
2.) Make sure that "matplotlib" is installed.

If both of these are not yet installed, please run the following commands in your terminal:
pip install pandas
pip install matplotlib
